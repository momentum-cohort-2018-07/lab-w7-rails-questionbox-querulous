<h4>Whiner index</h4>

<div id="body_container" class="body_wrapper">
<%= @whiners.each do |whiner| %>
    <div class="whiner_container">
        <div class="whiner_header"><%= whiner.username %><%# Whiner name %> 
        </div>
        <div class="whiner_body"><%= whiner.name %><%# List recent whines (and maybe answers) here %>
        </div>
        <div class="whiner_footer"><%= whiner.created_at %><%# Whiner since? something else? or not needed. Maybe put that in header %>
        </div>
    </div>
<% end %>
</div>